# 打开以前的 ChemDraw 文件

Structlnk 0.7.0，2026-09-18。

## 使用步骤

1. 重新打开 Structlnk，点击顶部「打开」。
2. 选择原来的 `.cdx` 或 `.cdxml` 文件；大写扩展名也可使用。
3. 在导入窗口中选择页面。多页文件每次选一页，其他页面可再次打开导入。
4. 查看转换预览、对象数量及提示，选择「常规绘图」或「ACS 1996 兼容样式」，再点击「打开所选页」。
5. 所选页直接进入主画布；使用画布四周的工具修改原子、键、文字、箭头和图片。
6. Ctrl+S 保存为 `.chemproj`。该文件可重新打开继续编辑；原 `.cdx` / `.cdxml` 文件不会被项目保存覆盖。

在导入窗口点击「取消」保留当前画布。打开所选页后可用 Ctrl+Z 撤销整次替换。

## 从剪贴板粘贴

1. 在 ChemDraw 中选中需要的分子或反应并按 Ctrl+C。
2. 回到 Structlnk，在画布中右键选择「粘贴」。该选项会自动识别 ChemDraw 原生结构；剪贴板中没有可编辑化学数据时显示灰色。
3. 检查导入预览和对象数量，然后点击「打开所选页」。

软件优先读取 `ChemDraw Interchange Format` 中的原生 CDX，也支持 CDXML、`Native` 内嵌 CDX 和 SMILES 回退。若剪贴板只有 EMF、PNG 等图片预览，软件会拒绝导入，因为这些图片不能继续按原子和键编辑。

## 能保留什么

- 由 Ketcher / Indigo 识别的原子、键、结构连接关系、立体化学信息、反应箭头、独立文字及部分图形。
- 转换成功的 PNG 等嵌入位图仍为图片，不会被自动识别为可编辑的化学结构。
- 所选页面转换为同一份 KET 主画布数据。分子、文字、箭头和受支持的图形可以在主画布中选择和编辑。
- 常规模式等比缩放以适应当前页面。ACS 1996 兼容样式使用本软件独立实现的默认标准键长与物理比例；导入后仍可拖动单个原子调整局部键长和键角。过大的内容会提示可能超出绘图区。导入页中的富文本仍受化学引擎的文字转换和排版规则影响。
- 使用公开格式信息进行页面选择与对象统计，化学转换使用随软件提供的开源引擎。没有增加 ChemDraw 运行依赖，也没有调用云端转换服务。

## 当前边界

这是可编辑导入功能，不是 ChemDraw 的无损替代。字体、段落、特殊键、机理弯箭头、鱼钩方向、箭头曲率、嵌套缩写、谱图、表格、OLE 附件及复杂排版可能不同或有内容未转换。数量提示只能发现部分问题，不能证明原稿已完整保留。导入后应对照原稿检查。

本版可以读取 ChemDraw 直接复制产生的原生剪贴板数据，并对 `Native` 中以标准 CDX 文件头开始的数据做兼容识别；没有保证从所有 Word / PowerPoint 版本及所有历史 OLE 对象恢复结构。截图、PDF、PNG 和仅含 EMF 的剪贴板不会被识别成结构。不提供整个主画布写回 CDX / CDXML 的无损往返。

一个 Structlnk 项目仍是一页。多页 ChemDraw 文件可以逐页导入并分别保存；CDX / CDXML 的逻辑页面可能与打印分页不同。输入文件与转换后的项目均受 40 MB 上限约束。

若某个 CDX 无法读取，可在 ChemDraw 中把所需页面另存为 CDXML 后再试。原文件没有被修改；损坏文件和没有可转换内容的页面不会替换当前画布。

## 验证范围

使用随已保留的 Indigo 1.46.0 源码提供的测试文件，以及自行生成的多页和金属配合物样本。实际覆盖了 ChemDraw 13 的二进制 CDX、ChemDraw 20 的 CDXML、UTF-16 CDXML、文字与反应箭头、嵌入位图、配位键方向、相对配位几何、增强立体化学标签、取消与撤销、保存后重开及重启恢复。

0.3.0 的 20 项导入检查通过；0.4.0 另验证 CDX / CDXML 可直接进入统一主画布并继续编辑。0.5.7 使用本机 ChemDraw 2022 自带的苯 CDX 样例验证 Windows 原生 ChemDraw 剪贴板读取，转换后得到 6 个原子和 6 条可编辑键。已检查可见导入窗口、导入后的画布和嵌入图片的 PNG 导出。所有测试使用独立测试数据目录，用户自动恢复文件保持不变。尚未收到用户实际绘图，不能据此保证所有历史 ChemDraw 对象均兼容。

详细检查记录见同目录 `Validation-Results.md`。

引擎格式接口参考：[Ketcher 官方项目说明](https://github.com/epam/ketcher/blob/master/README.md)。本次实际验证以本地 Ketcher 3.18.0 / Indigo 1.46.0 为准。
